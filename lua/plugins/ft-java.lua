return {
    -- TODO: Config JDTLS
    { 'mfussenegger/nvim-jdtls',                        -- Extensions for the built-in LSP support in Neovim for eclipse.jdt.ls 
        -- config = function() end,
    }
}
